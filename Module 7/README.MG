This script is used to classify images using a TensorFlow Keras model saved in .h5 format.
In addition to loading the model and labels, this script also preprocesses the image and returns prediction results in the form of class names and confidence scores.
